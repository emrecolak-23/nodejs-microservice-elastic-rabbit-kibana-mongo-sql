apiVersion: v1
kind: Service
metadata:
  name: jobber-mysql
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: jobber-mysql
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
